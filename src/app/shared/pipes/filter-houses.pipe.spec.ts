import { FilterHousesPipe } from './filter-houses.pipe';

describe('FilterHousesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHousesPipe();
    expect(pipe).toBeTruthy();
  });
});
